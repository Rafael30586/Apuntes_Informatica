POM son siglas de Project object model. Este archivo es el archivo principal de configuración de un proyecto de Maven. Define los metadatos del proyecto, dependencias, plugins y configuraciones de construcción requeridas para compilar, probar y sdesplegar una aplicación 

groupId y artifactId --> identifican tu proyecto entre todos los proyectos del mundo. Ejemplo de groupId: com.f-rafael. Ejemplo de artifactId: maven-tutorial

versión --> es la versión del proyecto. Si tiene la palabra Snapshot significa que es un trabajo en desarrollo

properties --> puede ir la versión de java y otras propiedades

build.plugins.plugin --> sirve para especificar plugins

dependencies.dependency --> es para las bibliotecas del proyecto que queremos que maven descargue. Cada biblioteca tiene las propiedades groupId, artifactId, versión y a veces otras más como scope. 

dependency.scope --> cuando una dependencia tiene compile en scope significa que necesitás la dependencia cuando compilás el proyecto, cuando ejecutás los tests y después también. scope también puede ser igual a test, lo que significa que solo necesitás la dependencia para ejecutar los tests, no la neceasitás cuando despliegues tu proyecto, por lo tanto que rés que se borre después 