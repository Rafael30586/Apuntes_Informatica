https://www.geeksforgeeks.org/operating-systems/virtual-memory-in-operating-system/

Memoria virtual en los sistemas operativos

Memoria virtual es una técnica de gestión de memoria usada por sistemas operativos para dar la apariencia de un bloque de memoria grande y continuo para aplicaciones, incluso cuando la memoria física (RAM) está limitada y no necesariamente asignada de una manera contigua:
* Un programa no necesita estar totalmente cargado en memoria para correr. Solo las partes necesarias son cargadas
* Los programas pueden ser más grandes que la memoria física disponible en el sistema
* La memoria virtual crea la ilusión de una memoria grande, incluso si la memoria RAM es pequeña
* Utiliza la RAM y el almacenamiento del disco para gestionar memoria, cargando solo las partes de programas a la RAM según la necesidad 
* Esto le permite al sistema correr más programas a la vez y gestionar la memoria de manera más eficiente

https://media.geeksforgeeks.org/wp-content/uploads/20250115142221545470/virtual_memory.webp

¿Cómo funciona la memoria virtual?
* La memoria virtual usa hardware y software para gestionar la memoria
* Cuando un programa corre, usa direcciones virtuales (no verdaderos lugares en memoria)
* La computadora convierte estas direcciones virtuales en direcciones físicas (verdaderos lugares en RAM) mientras el programa corre

------------------------
Tipos de memoria virtual

La memoria virtual es gestionada por la unidad de gestión de memoria (MMU), la cual suele estar integrada en el CPU. El CPU genera direcciones virtuales que el MMU traduce en direcciones físicas. Hay dos tipos principales de memoria virtual:
* Paginación (paging)
* Segmentación (segmentation) 

1. Paginación
Paginación divide la memoria en pequeños bloques de tamaño fijo llamados páginas (pages). Cuando a la computadora se le acaba la RAM, páginas que no están en uso actualmente son movidas al disco rígido, en una área llamada swap file.
* Las swap files actúan como una extensión de la RAM
* Cuando una página es necesitada otra vez, es cambiada otra vez a la RAM, un proceso conocido como cambio de página (page swapping)
* Esto asegura que el sistema operativo y las aplicaciones tengan suficiente memoria para correr (run)

Page fault service time: es el tiempo que toma para brindar servicio a la falla de página. El page fault service time incluye el tiempo que toma para llevar a cabo los pasos de arriba

____________________________________________________________________
No terminé de resumir porque no se entiende bien lo que quiere decir
____________________________________________________________________