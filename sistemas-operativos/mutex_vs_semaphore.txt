https://www.geeksforgeeks.org/operating-systems/mutex-vs-semaphore/

Mutex vs Semaphore

Mutex y semaphores son recursos del kernel que proveen sincronización de servicios (también conocidos como sichronization primitives). La sincronización es requerida cuando múltiples procesos se están ejecutando de manera concurrente, para evitar conflictos entre procesos usando recursos compartidos.

-------------------------
Mutex

Un mutex es diferente de un semáforo binario, que provee mecanismo de cerrado. Mutex es por "mutual exclusión object". Mutex es usado principalmente para proveer exclusión mutua a una porción específica del código para que el proceso se pueda ejecutar y trabajar con una sección particular del código en un momento particular.

https://media.geeksforgeeks.org/wp-content/uploads/20241015130619461685/Mutex.png

* Un mutex refuerza la posesión estricta. Solo el hilo que cierra el mutex puede abrirlo.
* Se usa específicamente para bloquear (lock) un recurso para asegurar que solo un hilo acceda a la vez
* Debido a esta estricta posesión, un mutex no se usa solamente de manera típica para señalización entre hilos, sino que se usa también para exclusión mutua también para asegurar que el recurso sea accedido solo por un hilo a la vez
* Mutex usa un mecanismo de herencia de prioridad para evitar problemas de inversión de prioridad. El mecanismo de herencia de prioridad mantiene a procesos de alta prioridad en un estado de bloqueado por el mínimo tiempo posible. De todas formas, esto no puede evitar el problema de inversión de prioridad, pero puede reducir su efecto hasta cierto punto

Usando Mutex

El problema del productor-consumidor: Considere el problema estándar del productor-consumidor. Asuma que tenemos un buffer de 4096 bytes. El productor del hilo recolecta los datos y los escribe en el buffer. Un hilo consumidor procesa los datos recolectados del buffer. El objetivo es que ambos hilos no corran al mismo tiempo
Solución: Un mutex provee exclusión mutua, el productor o el consumidor puede tener la llave (mutex) y proceder con su trabajo. Mientras el buffer esté lleno por el productor, el consumidor necesita esperar y viceversa. En cualquier momento, solo un hilo puede trabajar con el buffer entero. El concepto puede ser generalizado usando semaphore

Ventajas de mutex

* No emergen "race conditions", ya que solo un proceso está en la sección crítica a la vez
* Los datos permanecen consistentes y esto ayuda en mantener integridad
* Es un simple mecanismo de cerrado en la sección crítica y que se libera cuando deja la sección crítica

Desventajas de los mutex

* Si un hilo reteniendo el mutex se adelanta o va a dormir dentro de la sección crítica, otros hilos se bloquean, lo cual puede llevar a inversión de prioridad o inanición
* Los mutex involucran context switching y sobrecarga del kernel, lo que los hace más lentos que "spinlocks" para las secciones críticas muy cortas
* El uso incorrecto (como olvidar abrir) puede causar deadlock

-------------------
Semáforo

Un semáforo es una variable entera no negativa que es compartida entre varios hilos. El semáforo trabaja  sobre un mecanismo de señalización.

https://media.geeksforgeeks.org/wp-content/uploads/20241015130721027935/Semaphore.png

* Provee un mecanismo de control menos restrictivo. Cualquier hilo puede invocar signal() (también conocido  como release() o up()) y cualquier otro hilo puede invocar wait() (también conocido como acquire() o down())
* No hay propiedad estricta en los semáforos, lo que quiere decir que el hilo que señaliza no necesariamente tiene que ser el mismo que esperó

nota: Los semáforos son usados, generalmente, para señalización coordinada entre hilos. Los semáforos usan dos operaciones atómicas para sincronización de procesos: Wait (P) y Signal (V)

Usando semáforo

El problema del productor-consumidor: Considere el problema estándar del productor-consumidor. Asuma que tenemos un buffer de 4096 bytes. Un hilo productor recolecta los datos y los escribe en el buffer. Un hilo consumidor procesa los datos recolectados del buffer. El objetivo es que ambos hilos no deberían correr al mismo tiempo
Solución: un semáforo es un mutex generalizado. En lugar de un solo buffer, podemos dividir los 4 KB de buffer en 4 buffer de 1 KB (idénticos recursos). Un semáforo puede ser asociado con estos 4 buffer. El consumidor y el productor pueden trabajar en diferentes buffer al mismo tiempo

Ventajas de un semáforo

* Múltiples hilos pueden acceder a la sección crítica al mismo tiempo
* Los semáforos son independientes de la máquina
* Solo un proceso accederá a la sección crítica a la vez, aún así, múltiples hilos son permitidos
* Gestión flexible de recursos

Desventajas de un semáforo

* Tiene inversión de prioridad
* Las operaciones de semáforos (Wait, Signal) deben ser implementadas en la manera correcta para evitar deadlock
* Lleva a una pérdida de modularidad, por lo que los semáforos no pueden ser usados en sistemas a gran escala
* El semáforo es propenso a error de programación y esto puede llevar a deadlock o violación de la propiedad de exclusión mutua
* El sistema operativo tiene que rastrear todas las llamadas para operaciones wait y signal

-------------------------
Diferencias entre mutex y semáforo

* Mutex es un objeto y el semáforo es un número entero
* El mutex trabaja sobre el mecanismo de cerrado y el semáforo usa mecanismos de señalización
* Operaciones en mutex: cerrar (lock) y abrir (unlock);en semáforos: wait y signal
* Los mutex no tienen subtipos; semáforos hay de dos tipos: counting semaphore y semáforo binario
* Un mutex puede ser modificado solamente por el proceso que está solicitando o liberando el recurso; el semáforo funciona con dos operaciones atómicas (wait, signal) que pueden modificarlo
* Si el mutex está cerrado entonces el proceso necesita esperar en la cola de procesos y el mutex solo puede ser accedido una vez la cerradura se libera; Si el proceso necesita un recurso y ningún recurso está libre. Entonces el proceso necesita llevar a cabo una operación wait hasta que el valor del semáforo sea mayor que cero

------------------------
Ideas equivocadas sobre mutex y semáforo

Hay una ambigüedad entre semáforo binario y mutex. Podríamos llegar a creer que un mutex es un semáforo binario, ¡pero no lo es!. El propósito de un semáforo y un mutex es diferente.
* Un mutex esa un mecanismo de cerrado usado para sincronizar el acceso a un recurso
* Solo una tarea (puede ser un hilo o un proceso basado en la abstracción del SO) puede adquirir el mutex.
* Significa que hay una propiedad (ownership) asociada con el mutex y solo el dueño  puede liberar el cierre (mutex)
Mientras que un semáforo es un mecanismo de señalización usado para controlar el acceso  recursos compartidos en un sistema operativo. Por ejemplo, imaginá que estás bajando un archivo grande en tu computadora (tarea A) y simultáneamente tratando de imprimir un documento (tarea B). Cuando la impresión se inicia, gatilla a un semáforo que revisa si la descarga está completa
* Si la descarga está todavía en progreso, el semáforo previene a la tarea de impresión de continuar, como diciendo "esperá hasta que la descarga finalice"
* Una vez se completa la descarga, el semáforo señaliza que la tarea B puede empezara imprimir
* Esto asegura que ambas tareas no interfieran entre sí y ayuda a gestionar recursos de sistema eficientemente, permitiendo que las tareas se ejecuten de manera fluida sin conflicto